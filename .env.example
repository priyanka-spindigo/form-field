# Full Mailer embed POST URLs (from Form → Static websites → Create embed).
# Rebuild/redeploy after changing — values are inlined at build time.

PUBLIC_NEWSLETTER_SUBMIT_URL=
PUBLIC_QUICK_CONTACT_SUBMIT_URL=
PUBLIC_CONTACT_SUBMIT_URL=
